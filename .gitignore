# --- Go (GitHub template) ---
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
coverage.*
*.coverprofile
profile.cov
# vendor/
go.work
go.work.sum

# --- Secrets / env (NEVER commit — Vault is the only source; .claude/rules/secrets-vault.md) ---
.env
.env.*
*.tfvars
*.tfstate
*.tfstate.*
*.tfplan
secrets/

# --- Dependencies / build ---
node_modules/
.terraform/
coverage/
coverage.out

# --- Terraform state/plan (never commit — may carry secrets) ---
*.tfstate
*.tfstate.*
*.tfplan

# --- Sonar scanner working dir ---
.scannerwork/

# --- Tooling / generated ---
graphify-out/
.claude-flow/
.claude/worktrees/
.gstack/
.scannerwork/

# QG fixture artifacts
tests/qualitygate/*/coverage.out
tests/qualitygate/*/.scannerwork/
