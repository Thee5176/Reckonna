apiVersion: v1
kind: ServiceAccount
metadata:
  name: cloudflared
# Bound to the Vault k8s-auth role `reckonna-cloudflared` (provisioned human-side, S6).
# The Vault Agent Injector uses this SA identity to authenticate and render the tunnel token.
