apiVersion: v1
kind: Namespace
metadata:
  name: tailscale
  labels:
    app.kubernetes.io/name: tailscale-operator
    app.kubernetes.io/part-of: reckonna
    # NetworkPolicy in the postgres ns matches on this label.
    kubernetes.io/metadata.name: tailscale
